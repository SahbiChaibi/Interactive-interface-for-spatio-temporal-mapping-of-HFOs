load fich.ma
load fich2.mat
for jj=1:1:length
    for kk=1:length
        if(
            dep(ll)=
            fin(ll)=
            ll=ll+1;
            ind1(ll)=
        elseif()
            dep(ll)=
            fin(ll)=
            ll=ll+1;
            ind(ll)=
        elseif()
            dep(ll)=
            fin(ll)=
            ll=ll+1;
            ind11(ll)=
        elseif()
            dep(ll)=
            fin(ll)=
            ll=ll+1;
            ind(ll)=
            
        elseif()
            dep(ll)=
            fin(ll)=
            ll=ll+1;
            ind11(ll)=
        else()
            ind2(ll)=
        end
    end
end
            